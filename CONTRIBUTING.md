# Contributing

Read [CLAUDE.md](CLAUDE.md) before contributing. Its GitHub workflow applies to humans and AI coding assistants.

In short: create a task-specific branch before editing, keep changes scoped, run relevant checks, push only that branch, and open a pull request into `main`. Never commit secrets, real resumes, or candidate PII.
